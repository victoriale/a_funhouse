# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base             # Packages every Meteor app needs to have
mobile-experience       # Packages for a great mobile UX
mongo                   # The database Meteor supports right now
blaze-html-templates    # Compile .html files into Meteor Blaze views
session                 # Client-side reactive dictionary for your app
jquery                  # Helpful client-side library
tracker                 # Meteor's client-side reactive programming library

standard-minifiers      # JS/CSS minifiers run for production mode
es5-shim                # ECMAScript 5 compatibility for older browsers.
ecmascript              # Enable ECMAScript2015+ syntax in app code

autopublish             # Publish all data to the clients (for prototyping)
insecure                # Allow all DB writes from clients (for prototyping)
reactive-var
less
fortawesome:fontawesome
iron:router

accounts-base
accounts-password

meteorhacks:npm         # Complete NPM integration for Meteor
npm-container           # Supports meteorhacks:npm container packages.json

meteorhacks:ssr         # Server Side Rendering for Meteor
meteorhacks:picker      # Server Side Routing for Meteor
meteorhacks:kadira      # Allows Kadira to run on the the site for debugging
meteorhacks:kadira-profiler # Kadira CPU profiling for Meteor
meteorhacks:cluster     # Syncs to MongoDB for load balancing using nodes

spacebars               # handlebars-like template for Meteor(should be built in but just in case)
check                   # pattern checking functions useful for checking the types and structure of variables and an extensible library of patterns to specify which types you are expecting
momentjs:moment         # parse, validate, manipulate, and display dates
mrt:moment-timezone     # Timezone support for moment.js, packaged for Meteor. Currently includes all timezone data files
